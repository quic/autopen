#!/usr/bin/python
import MySQLdb
#import peewee
#from peewee import *
db = MySQLdb.connect(host="localhost", user="SHIFT", passwd="%&12SHilaE7", db="autopen")
cursor = db.cursor();

cursor.execute("SELECT * FROM cars");
numrows = cursor.rowcount
cars = cursor.fetchall()
#print cursor.fetchall()
v= 'Toyotaw'
if v in cars[0]:
	print 'Yes'
else:
	print 'no'
#for row in cars:
	#print row[0],row[1],row[2]
#row = cursor.fetchone()
#print row
#for x in range(0, numrows):
#	row = cursor.fetchone()
#	print row[0], "-->", row[1]
#db.close()
